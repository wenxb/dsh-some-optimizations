# 一些优化 bundle —— 把开关面板插件插入运行时。
# 两个优化都挂在全局 fetch 缝隙上,无需任何路由配置:
#   - normalizeCallIds      流内改写中转伪造的 tool-call id(防历史加载崩溃)
#   - unboundedStreamTimeouts 解除 300s 流式空闲超时(防长思考断流)
- insert:
    - id: some-optimizations
      name: 'dsh-some-optimizations'
